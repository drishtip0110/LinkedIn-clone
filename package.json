{
  "name": "linkedin-clone",
  "version": "1.0.0",
  "description": "Full-stack LinkedIn clone application",
  "scripts": {
    "dev": "./start-dev.sh",
    "test-backend": "node test-integration.js",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "axios": "^1.13.2"
  }
}